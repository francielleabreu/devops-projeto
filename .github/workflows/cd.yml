name: CI/CD Pipeline

on:
  push:
    branches: [ main ]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v3

    - name: Set up Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '18'

    - name: Install dependencies
      run: npm install

    - name: Run tests
      run: npm test

    - name: Build Docker image
      run: docker build -t ${{ secrets.DOCKER_USERNAME }}/devops-projeto:latest .

    - name: Log in to Docker Hub
      uses: docker/login-action@v2
      with:
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_PASSWORD }}

    - name: Push Docker image
      run: docker push ${{ secrets.DOCKER_USERNAME }}/devops-projeto:latest

    # - name: Deploy to AWS EC2
    #   env:
    #       EC2_IP: ${{ secrets.EC2_IP }}     
    #       EC2_USER: ubuntu                  
    #   run: |
    #     mkdir -p ~/.ssh
    #     echo "${{ secrets.EC2_SSH_KEY }}" > ~/.ssh/aws-key.pem
    #     chmod 600 ~/.ssh/aws-key.pem

    #     ssh -o StrictHostKeyChecking=no -i ~/.ssh/aws-key.pem $EC2_USER@$EC2_IP \
    #     "docker pull ${{ secrets.DOCKER_USERNAME }}/devops-projeto:latest && \
    #     docker stop app || true && docker rm app || true && \
    #     docker run -d --name app -p 80:3000 ${{ secrets.DOCKER_USERNAME }}/devops-projeto:latest"